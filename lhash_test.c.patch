--- openssl-4.0.0/test/lhash_test.c
+++ openssl-4.0.0/test/lhash_test.c
@@ -515,6 +515,9 @@
             num_workers = jobs;
     }
 
+    if (num_workers < 1)
+        num_workers = 1;
+
     TEST_info("using %d workers\n", num_workers);
 
     free(worker_exits);
